---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
  namespace: cert-manager
spec:
  acme:
    externalAccountBinding:
      keyID: ${EAB_KID}
      keySecretRef:
        name: zerossl-eabsecret
        key: secret
      keyAlgorithm: HS256
    

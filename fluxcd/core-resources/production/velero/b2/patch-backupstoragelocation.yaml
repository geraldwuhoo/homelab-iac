---
apiVersion: velero.io/v1
kind: BackupStorageLocation
metadata:
  name: velero
  namespace: velero
spec:
  config:
    region: us-west-004
    s3Url: "${S3_B2_URL}"
  credential:
    name: velero-b2
  backupSyncPeriod: 240m
  validationFrequency: 120m
  accessMode: ReadOnly

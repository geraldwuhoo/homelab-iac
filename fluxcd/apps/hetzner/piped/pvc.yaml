apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ytproxy-pvc
  namespace: piped
spec:
  accessModes:
  - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi

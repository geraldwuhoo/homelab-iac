apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ytproxy-pvc
  namespace: piped
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
